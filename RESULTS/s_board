#!/usr/bin/env bash

#SBATCH -J DIR_k
#SBATCH -N 1 --ntasks-per-node=1
#SBATCH -A mcdyfuzja
#SBATCH -p plgrid
#SBATCH --time=12:00:00

module load python-numpy
module load python-scipy


echo "start: "`date`
cd $SLURM_SUBMIT_DIR


ST=$PWD

#echo "preparing..."
./s_prep

#echo "binning..."
./s_run 0 "hist" "5" "5000"

#echo "averaging..."
./s_run 1 0.25 0.5 10.0

#echo "diffusiong..."
./s_run 2
